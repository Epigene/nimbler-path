require "spec_helper"

RSpec.describe NimblerPath do
  it "has a version number" do
    expect(NimblerPath::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
